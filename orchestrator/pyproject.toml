[project]
name = "orchestrator"
version = "0.1.0"
description = "python packages for dagster webserver and dagster deamon"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dagster>=1.13.15",
    "dagster-docker>=0.29.15",
    "dagster-graphql>=1.13.15",
    "dagster-postgres>=0.29.15",
    "dagster-webserver>=1.13.15",
]

